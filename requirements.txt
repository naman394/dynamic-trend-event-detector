# Core data science stack
requests>=2.28         # GDELT live feed HTTP downloads
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
nbconvert>=7.0
ipykernel>=6.0

# Deep Learning — SBERT + UMAP K-Means (implemented)
sentence-transformers>=2.2    # SBERT contextual embeddings
umap-learn>=0.5               # UMAP dimensionality reduction

# Topic coherence — requires Python ≤ 3.12 (use venv311 on this project)
gensim>=4.3

# LDA vs SBERT clusters (notebook 11 / src/topic_modeling_lda_bertopic.py)
hdbscan>=0.8
plotly>=5.0

# GDELT GKG in BigQuery (spike validation — optional)
google-cloud-bigquery>=3.14
google-cloud-bigquery-storage>=2.24  # optional: uses Storage Read API for to_dataframe(); without it, REST still fetches all rows (UserWarning only)
pandas-gbq>=0.19
db-dtypes>=1.2
pyarrow>=14.0  # used with BigQuery Storage path for faster/larger downloads

# ── Feature 1: Streamlit Interactive Dashboard ─────────────────────────────
streamlit>=1.33            # dashboard — run: streamlit run dashboard.py

# ── Feature 2: NER Entity Tracker ─────────────────────────────────────────
spacy>=3.7                 # NER pipeline
# After install, download a model (choose one):
#   python -m spacy download en_core_web_sm   (12 MB, fast)
#   python -m spacy download en_core_web_lg   (700 MB, most accurate)

# ── Feature 3: LSTM Autoencoder Anomaly Detector ──────────────────────────
torch>=2.0                 # PyTorch for LSTM autoencoder

# ── Feature 4: Granger Causality Network ──────────────────────────────────
statsmodels>=0.14          # grangercausalitytests
networkx>=3.0              # graph construction and layout
